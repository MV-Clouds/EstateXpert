<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Prevent_Listing_Type_Modification</fullName>
    <active>true</active>
    <description>Prevents users from changing the Listing Type after the record has been created.</description>
    <errorConditionFormula>AND(
    ISCHANGED(Listing_Type__c),
    NOT(ISBLANK(TEXT(PRIORVALUE(Listing_Type__c))))
)</errorConditionFormula>
    <errorDisplayField>Listing_Type__c</errorDisplayField>
    <errorMessage>The Listing Type cannot be changed once it has been set.</errorMessage>
</ValidationRule>
